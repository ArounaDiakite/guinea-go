# Deferred: no real WhatsApp/push provider integration yet (Firebase,
# WhatsApp Business API, ...). NotificationService currently only
# writes in_app notifications - build this out once real provider
# credentials exist, same sandbox-only stance as app/payments/
# providers/.

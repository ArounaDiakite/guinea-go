# Deferred: no real email provider integration yet (SendGrid, SES,
# ...). NotificationService currently only writes in_app notifications
# - build this out once real provider credentials exist, same sandbox-
# only stance as app/payments/providers/.

# Deferred: entirely empty scaffold (no schemas/service/repository/
# router). Not mounted in main.py or exposed by the API. Build this out
# only when a real need shows up (geo data beyond city/country) - see
# CLAUDE.md's "Données de référence" section.

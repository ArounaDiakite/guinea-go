# Deferred: only schemas.py exists (LanguageCreate/LanguageResponse).
# No service/repository, no router built, not mounted in main.py or
# exposed by the API. Build this out only when a real need shows up
# (e.g. filtering by preferred language) - see CLAUDE.md's "Données de
# référence" section.
